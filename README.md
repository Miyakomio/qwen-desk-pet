# 🎀 Qwen 二次元桌宠（祈りちゃん）

一个用 **Python + PySide6 (Qt/QML)** 做的本地二次元桌宠，调用你本地 Ollama 部署的 **qwen3:0.6b** 进行对话。

- 透明、无边框、置顶、可拖动的桌宠窗口（**小巧**，220×330）
- **现成透明桌宠素材**（绝区零「Codex」小机器人，**自带待机动画**，会轻轻晃动）
- **性格**：祈りちゃん，俏皮活泼、元气满满，爱撒娇冒颜文字
- **久坐提醒**：累计使用电脑 1/2/3/4/5 小时，各提醒一次休息
- **整点报时**：每到整点自动报当前时间（7/12/18/22 点等有定制台词）
- **触碰报时**：待机隐藏一段时间后，触碰/点击桌宠会实时报当前时间
- **终端执行**：在输入框输入 **`/+命令`**，祈りちゃん会打开终端执行该命令
- 打字机文字效果 + 聊天历史记忆（多轮上下文）
- **自动消息不干扰上下文**：久坐提醒、整点/触碰报时等系统消息只显示，不进入对话记忆，不影响连贯性
- **长对话自动压缩记忆**：聊久了会自动把较早内容总结成摘要，上下文更连贯
- **可展开的对话窗口**：点桌宠左上角「☰」打开独立对话记录窗口，查看全部历史、可滚动、可继续聊
- 根据回复内容自动切换表情
- **联网搜索**：说「搜/查/百度…」等词、或真正的问题时自动上网找结果并带来源；寒暄闲聊则不联网，保证对话连贯
- **可互动**：摸头、捏脸，会有表情和反应（独立弹幕，不打断对话）
- 系统托盘（可隐藏/唤出）

## ✨ 效果预览

- 角色在桌面底部，带轻微浮动动画
- 输入框在窗口底部，回车或点 ➤ 发送
- 气泡显示你的输入 + 祈りちゃん的回复（逐字打出）

## 📦 环境要求

- 已安装 Ollama，且本地已有模型 `qwen3:0.6b`（在 127.0.0.1:11434）
- Python 3.10+

## 🚀 使用方法

### 第一步：安装依赖（只需一次）

```bat
py -3 -m venv .venv
.\.venv\Scripts\python.exe -m pip install -r requirements.txt
```

### 启动（推荐）

**双击 `启动桌宠.bat`（或 `start_pet.bat`）** 即可——用 pythonw **静默启动，不弹黑控制台窗口**，第一次运行还会自动帮你建环境、装依赖。

想放桌面快捷方式？双击 **`创建桌面快捷方式.bat`**，会在桌面生成一个 `InoriPet.lnk`，以后一键启动。

### 备用：手动启动

```bat
.\.venv\Scripts\python.exe -m pet.main   :: 带控制台，方便看报错
```

> 前提：先启动 Ollama（`ollama serve`）并保证 `ollama list` 里有 `qwen3:0.6b`。

## 🖱️ 操作说明

| 操作 | 效果 |
|------|------|
| 拖动桌宠空白处（左键/右键都可） | 移动窗口 |
| **按住头顶（左键）** | 摸头：冒爱心，左边弹出开心回应（几秒后消失） |
| **点腮帮（左键）** | 捏脸：红印+抖动，左边弹出「别捏啦」 |
| 顶部中间 **「源」按钮** | 开关：回答是否附来源链接（亮=附，灰=不附） |
| 左上角 **☰** | 打开独立对话记录窗口（看全部历史、继续聊） |
| 右上角 ✕ | 隐藏窗口（去系统托盘） |
| 托盘图标 | 左键唤出，右键菜单：显示/隐藏、退出 |
| 底部输入框回车 / ➤ | 发送消息给祈りちゃん |

> 一段时间不互动后，对话气泡会先**变半透明**、再**完全隐身**（输入框和 ☰/✕ 按钮一起藏起）；**点击/触碰桌宠**就会恢复。正在打字或输入框有焦点时不会隐藏。

## 🌐 联网搜索

想让祈りちゃん上网查东西，说话里带上「**搜 / 搜索 / 查一下 / 百度 / 上网查**」等词，或问一个具体问题时即可，例如：

- 「帮我搜一下今天的新闻」
- 「查查 Python 是什么」
- 「百度一下明天北京天气」
- 「什么是智能指针」

祈りちゃん会用 DuckDuckGo / 中文维基百科搜索，把结果（含来源链接）反馈给你。

> **说明**：寒暄/闲聊（你好、哈哈、在吗、晚安…）会直接聊天、不联网，避免无关搜索干扰对话。可随时在 `pet/config.py` 里改 `SEARCH_ENABLED`、`SEARCH_ALWAYS`、`SEARCH_MAX_RESULTS`。

> 需要联网；使用公开接口，无需 API Key。

## 🖱️ 互动

桌宠不是只能对话，还能「碰」：

- **摸头**：鼠标按住角色头顶 → 冒爱心，脸旁弹出开心回应（几秒后消失）
- **捏脸**：点击角色腮帮 → 出现红印、抖动，脸旁弹出「别捏啦」

反应台词可在 `pet/config.py` 的 `PAT_REPLIES` / `POKE_REPLIES` 里改。

## 🎨 更换角色形象

桌宠支持**多套形象切换**，都是现成的透明桌宠素材，存放在 `pet/resources/pets/<名字>/` 下：

| 名字 | 说明 |
|------|------|
| `codex` | Codex 小机器人（8 帧**待机动画**，会晃动） |

**切换方法**：打开 `pet/config.py`，把 `PET_CHARACTER` 改成对应的名字，保存后重启桌宠即可。

**添加新形象**：
1. 在 `pet/resources/pets/` 新建一个文件夹（如 `mypet/`）
2. 把透明 PNG 帧放进去，命名为 `01.png`、`02.png`…
3. 在 `config.py` 的 `AVAILABLE_CHARACTERS` 里登记，再把 `PET_CHARACTER` 改成它
4. 多帧 = 自动循环待机动画；单帧 = 静态

素材来源（免费开源，禁止商用，个人使用没问题）：
- [mypet-codex-pet](https://github.com/leanyu165-gif/mypet-codex-pet)（Codex）

## 💻 终端执行命令

想让祈りちゃん帮忙在终端跑命令？在输入框输入 **`/+命令`**：

```
/+dir              → 打开终端执行 dir
/+ping 8.8.8.8     → 执行 ping
/+python --version → 执行 python --version
```

- 会**打开一个新的终端窗口**（cmd）执行该命令，执行后窗口保持打开，方便查看输出
- 这类命令**不入对话记忆**，不会影响上下文
- 可随时在 `pet/config.py` 关掉：`TERMINAL_ENABLED = False`

> ⚠️ 这会直接执行你输入的命令，请确保命令安全。

## ⚙️ 配置

所有可调参数都在 `pet/config.py`：

| 参数 | 说明 | 默认 |
|------|------|------|
| `MODEL` | 模型名 | `qwen3:0.6b` |
| `OLLAMA_BASE` | Ollama 地址 | `http://127.0.0.1:11434` |
| `TEMPERATURE` | 采样温度 | `0.7` |
| `MAX_HISTORY_TURNS` | 最近保留对话轮数（更久远自动压缩成摘要） | `10` |
| `TYPEWRITER_*` | 打字机速度 | 每 14ms 打 1 字 |
| `WINDOW_WIDTH/HEIGHT` | 窗口尺寸（更小省空间） | `220×330` |
| `SEARCH_ENABLED` | 联网搜索开关 | `True` |
| `SEARCH_ALWAYS` | 真问题是否都先联网再回答 | `True` |
| `SHOW_SOURCES` | 回复末尾是否附来源（默认关） | `False` |
| `IDLE_SEMI_MS` | 无互动多久后气泡变半透明 | `6000` |
| `IDLE_HIDE_MS` | 无互动多久后气泡/输入框/按钮隐身 | `10000` |
| `BREAK_REMIND_ENABLED` | 久坐提醒开关 | `True` |
| `BREAK_HOURS` | 累计使用到这些小时各提醒一次 | `(1,2,3,4,5)` |
| `REPORT_TIME_ENABLED` | 触碰报时开关 | `True` |
| `REPORT_TIME_AFTER_IDLE_MS` | 待机多久后触碰才报时 | `10000` |
| `CHIME_ENABLED` | 整点报时开关 | `True` |
| `TERMINAL_ENABLED` | 终端命令（`/+`）开关 | `True` |
| `PAT_REPLIES`/`POKE_REPLIES` | 摸头/捏脸台词 | 见文件 |
| `SYSTEM_PROMPT` | 祈りちゃん的人设 | 见文件 |

换模型只需改 `MODEL`（比如换成更大的 `qwen2.5:7b`）。

## 🗂️ 项目结构

```
pet/
├── main.py          # 入口（窗口 + 托盘 + 形象加载）
├── config.py        # 配置
├── bridge.py        # Python <-> QML 桥接（对话/搜索/命令/提醒）
├── core/
│   ├── memory.py        # 聊天记忆（含自动压缩摘要）
│   ├── emotion.py       # 表情判定
│   ├── message_model.py # 对话记录列表模型（含时间戳）
│   ├── usage.py         # 系统闲置时间检测（久坐提醒）
│   └── terminal.py      # 终端命令执行（/+）
├── llm/
│   └── client.py    # Ollama 调用（后台线程 + 摘要压缩）
├── search/
│   └── client.py    # 联网搜索（DuckDuckGo + 维基回退）
├── resources/
│   └── pets/        # 桌宠形象素材帧
└── qml/
    ├── PetWindow.qml    # 主窗口（紧凑布局、隐身、互动）
    ├── Character.qml    # 角色（待机动画 + 摸头/捏脸）
    ├── ChatBubble.qml   # 气泡（打字机 + 滚动 + 思考动画）
    └── ChatWindow.qml   # 展开的对话记录窗口
```

## 🔮 后续规划（电脑控制模块预留）

本版专注「桌宠 + 对话」，未实现电脑控制。架构上已把控制逻辑与对话解耦，后续可通过「意图识别 + 白名单动作」加入：

- 打开软件 / 文件 / 文件夹
- 打开网址、系统搜索
- 音量 / 亮度调节、系统状态查询
- 锁屏 / 关机 / 重启（敏感操作弹窗确认）

如果你需要，我可以下一步把「电脑控制」功能加上。
